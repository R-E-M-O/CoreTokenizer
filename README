Files included:
1. CoreScanner.py - Contains the CoreScanner class responsible for tokenization and scanning operations.
2. main.py - runs the Interpreter on the provided program file with data from the provided data file
3. BNFClasses.py - Contains the Parse, Print, Execute methods for the Interpreter

Instructions:
1. Ensure you have Python installed on your system.
2. Place BNFClasses.py CoreScanner.py, and main.py in your working directory.
3. To execute the program, use the command `python main.py [PROGRAM_FILE_NAME] [DATA_FILE_NAME]`. Replace `[PROGRAM_FILE_NAME] [DATA_FILE_NAME]` with the program you wish to execute followed by the file whose data you would like to provide to it.
   Example: `python main.py InterpreterTest1.txt DataForInterpreterTest1.txt`
4. The program will parse the program first, then print it to the screen, then execute and show the output to the screen.


